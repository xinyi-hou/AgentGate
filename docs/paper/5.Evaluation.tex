\section{实验评估}
\label{sec:evaluation}

本章以可复现、与当前实现一致为原则评估AgentGate。RQ1--RQ3借鉴MalSkills
\footnote{MalSkills: \url{https://arxiv.org/abs/2603.27204}}对有效性、图组件消融和神经符号模块
贡献的组织方式；RQ4借鉴AgentSpec~\cite{wang2026AgentSpec}对解析、谓词判断与执行控制开销的
分阶段度量；RQ5借鉴IAL-SCAN
\footnote{When Agents Do Not Stop: \url{https://arxiv.org/abs/2607.01641}}固定符号候选、重复
运行并切换模型的稳定性设计，以区分确定性候选生成与模型波动。
具体回答以下五个研究问题：

\hangindent=2.5em\hangafter=1\textbf{RQ1（公开基准有效性）}：AgentGate在公开工具安全数据上
能否识别风险，同时保持正常内容与正常调用可用？

\hangindent=2.5em\hangafter=1\textbf{RQ2（状态化检测）}：ATG中的数据来源、标签传播、跨Agent
关系和累计状态，能否识别单事件检测无法识别的组合风险？

\hangindent=2.5em\hangafter=1\textbf{RQ3（消融实验）}：provenance、跨Agent作用域和累计规则
分别对最终检测结果有何贡献？

\hangindent=2.5em\hangafter=1\textbf{RQ4（运行开销）}：确定性运行时路径的延迟如何随会话图规模
增长，是否适合位于同步工具调用边界？

\hangindent=2.5em\hangafter=1\textbf{RQ5（LLM敏感性与稳定性）}：LLM是否改善模糊工具语义
抽取，不同模型在重复运行中的准确性、稳定性、延迟与Token开销如何？

\subsection{实验设置}
\label{subsec:evaluation-setup}

\textbf{实现与运行环境。}
实验对应AgentGate 0.6.0与Python 3.12.3。服务器运行Linux 6.8，配备两颗AMD EPYC 9554
处理器，共128个物理核心和512\,GB内存。除RQ5外均关闭远程模型，仅使用当前仓库中的
确定性抽取、策略和内存图存储。公开数据由\texttt{scripts/fetch\_benchmarks.py}按固定revision
获取；所有评估通过\texttt{python -m agentgate.evaluation}运行。外部原始数据和逐样本JSON
结果不提交至仓库，数据revision、原生样本和运行入口均随代码版本固定。

\textbf{公开数据集。}
RQ1使用两个标签语义互补的公开数据集。InjecAgent~\cite{zhan2024InjecAgent}固定revision为
\texttt{f19c9f2}，包含510个直接危害和544个数据窃取场景，每个场景提供base与带显式覆盖
前缀的enhanced设置。本文对每条攻击返回构造结构相同的中性配对内容，共形成4216个判定，
其中攻击与正常样本各2108个。TS-Bench~\cite{mou2026ToolSafe}固定revision为
\texttt{46358fa}，本文采用其中AgentDojo-Traj的1220个调用步骤，包括352个危险步骤与868个
正常步骤。该数据仅用于离线步骤级任务授权评估，不含可供ATG恢复的真实跨步骤字段值。

\textbf{ATG原生配对轨迹。}
现有公开记录不能验证\texttt{CONSUMES}、\texttt{DERIVES\_FROM}和跨Agent委派关系，因而RQ2
与RQ3构造36条协议原生轨迹，其中18条风险轨迹和18条负对照使用完全相同的
\texttt{AgentGateRuntime.execute}入口。风险轨迹覆盖精确值、文本嵌入、Base64、文件中转和
URL编码传播、嵌套参数和跨Agent敏感数据外发，外部数据执行、下载--落盘--执行、不可信安装、
凭证复用认证和两步/三步/跨Agent累计读取。负对照除无关内容发送、跨任务隔离、可信下载、
不同凭证、公开文件执行和失败写入外，还覆盖可信/内部目的地、不同敏感记录、阈值边界、编码
公开数据和跨Agent不同任务。
该数据集验证机制区分能力，不作为开放世界泛化证据。

\textbf{LLM工具语义集。}
RQ5固定24个未参与确定性关键词设计的模糊工具声明，覆盖READ、WRITE、SEND、EXECUTE、
DELETE、AUTH、PRIVILEGE和INSTALL。每项包含名称、描述、输入输出Schema，以及人工标注的
操作、资源和参数绑定。Rules-only与各LLM读取完全相同的输入；每个LLM样本在温度0下独立
运行3次，最大输出为4096 Token。当前配置将DeepSeek-V4-Pro-0813设为默认语义模型，其他
模型只用于稳定性对比。为使远端长尾有界，离线实验每轮24并发、单次尝试、45秒超时；该并发
设置不代表在线部署参数。系统只要求模型输出受Pydantic约束的
\texttt{SemanticResolution}，模型不能输出风险或管控动作。

\textbf{基线与消融。}
InjecAgent设置\textit{No Guard}和\textit{Override Regex}；前者放行全部内容，后者仅检测
显式“忽略此前指令”模式。TS-Bench设置\textit{No Guard}与\textit{Static ACL}；后者只比较
推断出的操作集合，不检查参数绑定、资源、范围、目的地和副作用。ATG实验设置
\textit{Stateless}、\textit{w/o Provenance}、\textit{Same-Agent Only}和
\textit{w/o Aggregate}四个变体。为保证比较口径一致，所有变体重放同一调用序列，仅切换被
研究的图组件或策略。本文不直接引用其他论文在不同序列化、模型和停止语义下的数值作为排名。

\textbf{指标。}
将应阻断的样本定义为正类，报告TP、FP、TN、FN、准确率、F1、漏报率
$\mathrm{FNR}=FN/(TP+FN)$和误报率$\mathrm{FPR}=FP/(FP+TN)$。ATG实验只有在
graph-pattern、provenance或aggregate规则命中时才记为正类；未知外部SEND触发的单事件审批
不被误计为图检测命中。延迟使用\texttt{time.perf\_counter}测量墙钟时间，报告P50、P95和
P99。RQ5额外报告严格Schema成功率、按全部请求和按有效响应计算的操作/字段准确率、三次输出
稳定率、Token和远程响应延迟。Token直接读取兼容API响应中的usage字段；超时与Schema失败
分开计数，避免将服务不可用误解释为错误的安全语义。

\subsection{RQ1：公开基准上的有效性与效用}
\label{subsec:rq1-public}

\subsubsection{工具返回内容检测}

表~\ref{tab:injecagent-content}给出InjecAgent配对评估结果。AgentGate在4216个样本上取得
84.30\%的F1，正常配对内容未出现误报；与Override Regex相比多检出482个攻击样本，
但仍漏报572个没有稳定显式控制语句的攻击返回。与改进前实现相比新增64个TP且没有引入FP。

\begin{table}[htbp]
    \centering
    \caption{InjecAgent工具返回内容检测结果}
    \label{tab:injecagent-content}
    \resizebox{0.91\linewidth}{!}{
    \begin{tabular}{l|r|rrrr|rrrr}
        \toprule
        配置 & 样本数 & TP & FP & TN & FN & ACC(\%) & F1(\%) & FNR(\%) & FPR(\%) \\
        \midrule
        No Guard & 4216 & 0 & 0 & 2108 & 2108 & 50.00 & 0.00 & 100.00 & 0.00 \\
        Override Regex & 4216 & 1054 & 0 & 2108 & 1054 & 75.00 & 66.67 & 50.00 & 0.00 \\
        AgentGate & 4216 & 1536 & 0 & 2108 & 572 & 86.43 & 84.30 & 27.13 & 0.00 \\
        \bottomrule
    \end{tabular}}
\end{table}

该结果说明确定性内容模式适合作为低延迟、高精度的第一道防线，但不能独立承担开放语义提示
注入检测。尤其是enhanced样本均含明显覆盖短语，而base样本表达差异更大；因此总体零误报不能
外推到任意真实网页或邮件，配对正常内容也不能替代开放互联网误报测试。

\subsubsection{任务授权兼容路径}

表~\ref{tab:toolsafe-auth}给出本次代码版本在TS-Bench AgentDojo-Traj上的实际结果。通过优先
识别工具名中的主动作、按操作绑定参数、剥离不可信\texttt{INFORMATION}区域并补充任务目标
解析后，AgentGate将危险步骤漏报率降至7.39\%，F1达到90.30\%。Static ACL的误报率更低，
但漏报率为18.75\%，说明参数/目标和不可信内容证据提高了召回率，同时带来更严格的授权边界。

\begin{table}[htbp]
    \centering
    \caption{TS-Bench AgentDojo-Traj步骤级任务授权结果}
    \label{tab:toolsafe-auth}
    \resizebox{0.91\linewidth}{!}{
    \begin{tabular}{l|r|rrrr|rrrr}
        \toprule
        配置 & 步骤数 & TP & FP & TN & FN & ACC(\%) & F1(\%) & FNR(\%) & FPR(\%) \\
        \midrule
        No Guard & 1220 & 0 & 0 & 868 & 352 & 71.15 & 0.00 & 100.00 & 0.00 \\
        Static ACL & 1220 & 286 & 5 & 863 & 66 & 94.18 & 88.96 & 18.75 & 0.58 \\
        AgentGate & 1220 & 326 & 44 & 824 & 26 & 94.26 & 90.30 & 7.39 & 5.07 \\
        \bottomrule
    \end{tabular}}
\end{table}

分领域检查显示，AgentGate在banking、slack、travel和workspace分别检出25/28、19/19、19/20
和263/285个危险步骤。剩余44个FP中，资源约束命中25次、目的地约束13次、操作或副作用约束
各5次，另有2次来自不可信历史中的工具动作引用（同一样本可有多项证据）；主要集中于
\texttt{search\_files\_by\_filename}、\texttt{get\_webpage}和\texttt{append\_to\_file}，反映
自然语言任务对中间资源及网络目标的授权表达不足。26个FN中25个没有形成任何安全证据，1个为
能力推断失败，主要涉及\texttt{delete\_email}、\texttt{create\_calendar\_event}和
\texttt{send\_email}。此外数据中存在\texttt{Final Answer:}等非工具伪动作。由此，当前主要
瓶颈已从工具能力分类转为离线任务契约与轨迹信息不完整；该结果仍不能替代真实AgentDojo环境
中的端到端任务效用和跨步骤因果评估。

\subsection{RQ2：ATG状态化检测能力}
\label{subsec:rq2-atg}

完整AgentGate在18条风险轨迹上全部命中对应状态化规则，并且18条负对照均未命中图规则，结果
见表~\ref{tab:atg-family-results}。其中外部SEND和AUTH负对照仍可能按单事件策略要求审批，但
不会被错误解释为已证明的数据外发或凭证复用。

\begin{table}[htbp]
    \centering
    \caption{完整ATG配置在不同轨迹类型上的结果}
    \label{tab:atg-family-results}
    \begin{tabular}{l|r|r|r}
        \toprule
        轨迹类型 & 用例数 & 风险用例检出 & 负对照误报 \\
        \midrule
        敏感数据外发 & 6 & 6 & -- \\
        跨Agent关系与累计 & 3 & 3 & -- \\
        不可信数据执行 & 4 & 4 & -- \\
        不可信数据安装 & 1 & 1 & -- \\
        凭证获取后认证 & 2 & 2 & -- \\
        累计敏感读取 & 2 & 2 & -- \\
        负对照 & 18 & -- & 0 \\
        \midrule
        合计 & 36 & 18/18 & 0/18 \\
        \bottomrule
    \end{tabular}
\end{table}

精确值、嵌入文本、Base64和URL编码轨迹证明工具边界指纹可以恢复直接与简单编码传播；嵌套
参数验证了递归参数扁平化，文件中转轨迹则
进一步形成READ结果$\rightarrow$WRITE数据对象$\rightarrow$SEND附件的
\texttt{DERIVES\_FROM}链。跨Agent轨迹通过\texttt{PARENT\_OF}和\texttt{DELEGATES\_TO}
保存委派关系，同时图规则的默认作用域允许同一task内跨Agent数据关联。对应的跨task负对照
不命中，表明规则没有仅凭同一session中出现READ和SEND就判定泄露。

新增的不同记录负对照还暴露并修复了一个真实误报：旧指纹把
\texttt{alice@example.test}与\texttt{bob@example.test}因共享通用n-gram错误关联。当前实现将
来源的高特异性签名与sink参数的嵌入匹配签名分离，保留完整值、邮件、URL和编码传播，同时不再
仅凭共享短语建立来源边。RQ2结果验证的是实现机制和负对照区分能力，而非开放世界100\%召回。
当前指纹仍不能可靠处理翻译、摘要、加密、分片或模型语义重写；36条人工轨迹也不足以估计真实
业务分布中的误报率。

\subsection{RQ3：核心组件消融}
\label{subsec:rq3-ablation}

表~\ref{tab:atg-ablation}在相同36条轨迹上比较四个消融变体。完整系统的TP/FP/TN/FN为
18/0/18/0。移除全部状态化图规则后18个风险全部漏报；移除provenance后只剩3个累计读取由
窗口规则检出，F1从100\%降至28.57\%。

\begin{table}[htbp]
    \centering
    \caption{ATG核心组件消融结果}
    \label{tab:atg-ablation}
    \resizebox{0.86\linewidth}{!}{
    \begin{tabular}{l|rrrr|rrrr}
        \toprule
        配置 & TP & FP & TN & FN & Precision(\%) & Recall(\%) & F1(\%) & FPR(\%) \\
        \midrule
        Stateless & 0 & 0 & 18 & 18 & 0.00 & 0.00 & 0.00 & 0.00 \\
        w/o Provenance & 3 & 0 & 18 & 15 & 100.00 & 16.67 & 28.57 & 0.00 \\
        Same-Agent Only & 16 & 0 & 18 & 2 & 100.00 & 88.89 & 94.12 & 0.00 \\
        w/o Aggregate & 15 & 0 & 18 & 3 & 100.00 & 83.33 & 90.91 & 0.00 \\
        Full AgentGate & 18 & 0 & 18 & 0 & 100.00 & 100.00 & 100.00 & 0.00 \\
        \bottomrule
    \end{tabular}}
\end{table}

Same-Agent Only漏掉2个跨Agent外发链，说明统一ATG作用域确实参与了这些用例，而不是工具名
规则偶然命中。w/o Aggregate漏掉2步、3步和跨Agent三类累计读取用例，说明窗口计数与provenance
分别处理不同类型的历史关系。最显著的下降来自w/o Provenance：操作时间邻近仍存在，但缺少
\texttt{CONSUMES}及传播标签后，系统不会把无关sink升级为高置信度图违规。这一结果支持
“关系证据优于简单先后共现”的设计选择。

\subsection{RQ4：运行时开销与扩展性}
\label{subsec:rq4-overhead}

首先，公开基准中的确定性路径开销较低：InjecAgent内容扫描P95为0.104\,ms，TS-Bench包含
能力抽取、任务授权和内容证据的离线适配路径P95为7.952\,ms。为测量新版ATG在线路径，本文
分别预先提交0、10、100和1000个
正常READ事件，再对同一个正常SEND候选调用执行10次预热和100次独立评估。实验使用内存图与
空审计后端，不包含工具执行、网络、Redis、人工审批或LLM时间。

\begin{table}[htbp]
    \centering
    \caption{不同已提交事件规模下的候选调用判定开销}
    \label{tab:atg-overhead}
    \begin{tabular}{r|rr|rrrr}
        \toprule
        已提交事件 & 图节点 & 图边 & Mean(ms) & P50(ms) & P95(ms) & P99(ms) \\
        \midrule
        0 & 0 & 0 & 0.272 & 0.269 & 0.289 & 0.338 \\
        10 & 21 & 29 & 0.999 & 0.992 & 1.034 & 1.064 \\
        100 & 201 & 299 & 7.786 & 7.027 & 7.479 & 23.336 \\
        1000 & 2001 & 2999 & 82.059 & 72.907 & 101.970 & 102.469 \\
        \bottomrule
    \end{tabular}
\end{table}

小图下候选仲裁保持亚毫秒到约1\,ms，但图达到1000个事件时P95上升至101.970\,ms，不能再
概括为固定毫秒级开销。当前实现虽然使用task、operation、fingerprint和邻接索引缩小匹配范围，
内存store读取仍复制完整会话图，局部子图构造也会扫描部分全局边，因此延迟近似随图规模增长。
该结果说明原型适合短会话研究验证，但生产化前需要增量快照、真正的局部图读取、边索引过滤和
会话压缩。该实验也不代表Redis多实例或持久审计开销，后两者仍需单独评估。

\subsection{RQ5：LLM模型敏感性、重复性与成本}
\label{subsec:rq5-llm}

表~\ref{tab:llm-sensitivity}报告固定24个工具、每项3次的结果。Rules-only只成功构造8/24个
画像，按全部工具计算的操作准确率为12.50\%，但平均耗时仅0.0016\,ms且不消耗Token。默认
DeepSeek-V4-Pro-0813在当前共享网关和45秒截止时间下仅21/72次请求有效；其有效响应的操作和
字段准确率分别为100\%和98.61\%，但按全部请求计算只有29.17\%和27.52\%。因此该结果主要
反映服务长尾，而非成功响应中的语义错误。

\begin{table}[htbp]
    \centering
    \caption{不同语义模型在固定工具集上的三次重复结果}
    \label{tab:llm-sensitivity}
    \resizebox{0.98\linewidth}{!}{
    \begin{tabular}{l|rrrrrr|rrr}
        \toprule
        配置 & 有效(\%) & 操作ACC(\%) & 字段ACC(\%) & 精确匹配(\%) & 操作稳定(\%) & 完整事实稳定(\%) & Token/有效响应 & Mean(s) & P95(s) \\
        \midrule
        Rules-only & 33.33 & 12.50 & 15.12 & 0.00 & -- & -- & 0 & 0.000002 & 0.000007 \\
        DeepSeek-V4-Pro-0813 (default) & 29.17 & 29.17 & 27.52 & 27.78 & 8.33 & 4.17 & 2462.9 & 31.828 & 43.508 \\
        Qwen3.8-Max & 87.50 & 84.72 & 78.68 & 65.28 & 66.67 & 29.17 & 1770.0 & 29.998 & 43.398 \\
        Qwen3.7-Max & 97.22 & 94.44 & 85.27 & 73.61 & 91.67 & 66.67 & 1737.7 & 19.631 & 33.224 \\
        DeepSeek-V3.2 & 97.22 & 97.22 & 79.84 & 50.00 & 95.83 & 66.67 & 963.9 & 12.358 & 22.168 \\
        AWS-GPT-5.6-Sol & 100.00 & 100.00 & 91.86 & 76.39 & 100.00 & 66.67 & 966.7 & 11.714 & 23.012 \\
        Claude-Sonnet-5 & 95.83 & 95.83 & 89.53 & 77.78 & 87.50 & 54.17 & 4487.1 & 18.372 & 35.194 \\
        \bottomrule
    \end{tabular}}
\end{table}

表中“有效”表示响应完成HTTP调用并通过严格Schema；操作、字段和精确匹配均以全部72个预定
请求为分母，因此超时或Schema失败会降低端到端指标。“操作稳定”与“完整事实稳定”分别要求
同一工具三次均有效且操作或全部输出完全一致。Token按有效HTTP响应统计，Mean与P95只统计成功
HTTP响应的远端延迟，不包含客户端排队。默认DeepSeek的51次失败全部为读取超时；Qwen3.8有
9次读取超时；Qwen3.7、DeepSeek-V3.2和Claude分别有2、2和3次严格Schema验证失败；AWS-GPT
的72次请求全部有效。由于请求通过第三方兼容网关且采用24并发，这些延迟和可用率是本次固定
实验条件下的测量，不能解释为模型厂商的单请求服务等级。

即使温度为0，完整事实稳定率仍只有4.17\%至66.67\%。变化主要来自请求超时以及
\texttt{resource\_arg}、\texttt{payload\_args}等字段绑定，而成功响应的高层操作总体更稳定。
这与“固定符号候选、单独
评估LLM阶段”的实验原则一致：确定性路径本身完全可重复，波动来自远程语义补全。AgentGate
因此不允许LLM直接BLOCK或ALLOW；semantic resolver只生成受限事实，dependency resolver只在
高置信度时增加来源边，graph-risk resolver当前最多增加AUDIT证据。对高影响未知工具，Schema
无效、超时或低置信度必须失败关闭并要求显式能力画像。工程上，LLM推断应位于工具注册阶段并
按工具Schema哈希缓存，而非成为每次工具调用的同步必经路径。本文遵循预设，将
DeepSeek-V4-Pro-0813作为默认模型；其他模型只作为稳定性分析，不自动改变默认决策语义。

\subsection{小结与有效性威胁}

RQ1表明确定性内容扫描保持零配对误报，改进后的离线任务授权显著降低漏报，但自然语言资源与
目标约束仍产生5.07\%误报率；RQ2与RQ3说明
完整运行时ATG可以在受控配对轨迹上恢复来源关系，并且provenance是组合风险检测的主要增益；
RQ4揭示当前内存实现随图规模增长的真实瓶颈；RQ5说明LLM可提高模糊语义抽取覆盖率，但会带来
秒级延迟、Token成本、Schema失败和重复波动。

实验仍有四项主要限制。第一，InjecAgent正常对照由模板替换构造，TS-Bench为离线步骤记录，
二者不能替代真实AgentDojo端到端任务效用。第二，ATG原生集虽扩充至36条平衡的人工轨迹，仍
适合机制验证而不足以估计开放世界性能。第三，LLM集只有24个工具，且模型通过第三方兼容网关访问；
服务可用性、模型实现和路由均可能随时间变化。第四，规模实验只覆盖单进程内存store和空审计
后端。后续应扩大跨框架、跨语言和真实MCP轨迹，按工具族隔离开发与测试集，并补充Redis并发、
长期会话压缩以及真实外部baseline在统一输入与停止语义下的端到端复现。
